version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
  jobs: 
    post_build: 
    - echo "Command run at 'post_build' step" 
    - echo `date`

python:
  install:
    - requirements: requirements.txt

sphinx:
  fail_on_warning: false